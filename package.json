{
  "name": "carebrain-patient-qa",
  "private": true,
  "scripts": {
    "db:schema": "node database/run-schema.js",
    "db:seed": "node database/seed-from-csv.js",
    "db:setup": "npm run db:schema && npm run db:seed",
    "backend:dev": "npm --prefix backend run start:dev",
    "backend:test": "npm --prefix backend test",
    "frontend:dev": "npm --prefix frontend start",
    "evaluate": "npm --prefix backend run evaluate"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
